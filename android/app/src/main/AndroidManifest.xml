<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required: all API calls to Render, image loads from Cloudinary -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required: lets the app prompt Android's installer for self-updates -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Required on Android 13+ (API 33) to show push/local notifications;
         requested at runtime via FirebaseMessaging.requestPermission() in
         push_service.dart, but the manifest entry is required regardless. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required so a killed/backgrounded app can still receive FCM
         messages and wake to process them. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:label="EnvyEnhance"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Default channel/icon for notifications delivered while the
             app is backgrounded/killed (FCM auto-displays using these
             when there's no foreground handler to intercept it — see
             push_service.dart for the foreground case). Must match the
             channel id created in push_service.dart's _androidChannel. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="envyenhance_default" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
    </application>
</manifest>
